# Is any external secret operator (ESO / Vault / SealedSecrets) installed here?
$ kubectl get crds 2>/dev/null | grep -i -E 'secret|vault' || echo 'Standard native Kubernetes Secrets in use (no external secret operator CRDs)'
Standard native Kubernetes Secrets in use (no external secret operator CRDs)

# native secret storage: etcd, base64-encoded, NOT encrypted at rest by default.
$ kubectl get secret yatri-db-secret -o jsonpath='{.type}{"\n"}'
Opaque

# Anyone holding 'get secrets' RBAC in this namespace can read the plaintext:
$ kubectl auth can-i get secrets
yes

# -> which is exactly why production clusters pull secrets from an external store.
